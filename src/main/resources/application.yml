server:
  port: 8000
spring:
  application:
    name: exchange-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/exchange_service
    username: root
    password: 68270895
  logging:
    level:
      root: warn
      br.com.luizmrd.Startup: debug
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false


